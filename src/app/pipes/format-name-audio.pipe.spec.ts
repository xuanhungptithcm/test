import { FormatNameAudioPipe } from './format-name-audio.pipe';

describe('FormatNameAudioPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatNameAudioPipe();
    expect(pipe).toBeTruthy();
  });
});
